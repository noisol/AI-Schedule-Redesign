# AI 일정 재설계 서비스 개발 명세서

---

> Version 1.0 (MVP)
> 
> 
> 작성 목적: AI 기반 일정 재설계 서비스 개발을 위한 프로젝트 명세서
> 

---

# 1. 프로젝트 개요

## 1.1 프로젝트 소개

AI 일정 재설계 서비스는 단순히 일정을 생성하는 서비스가 아니라, 사용자의 하루가 예상과 다르게 흘러갈 때 남은 일정을 가장 효율적으로 다시 설계해 주는 AI 서비스이다.

사용자는 오늘의 일정을 직접 입력하고, 예상치 못한 상황이 발생했을 때 자연어로 현재 상황을 입력한다. AI는 기존 일정과 사용자의 입력을 분석하여 남은 하루를 최적화한 새로운 일정을 제안한다.

---

## 1.2 문제 정의

기존 일정 관리 서비스는 일정 등록, 수정, 알림 기능에 초점을 맞추고 있다.

하지만 실제 일상에서는 다음과 같은 상황이 자주 발생한다.

- 과제가 예상보다 오래 걸리는 경우
- 갑작스럽게 새로운 일정이 생긴 경우
- 약속 시간이 변경된 경우
- 일정 수행이 불가능해진 경우
- 예상보다 빨리 일정이 끝난 경우

이러한 상황에서는 사용자가 남은 일정을 직접 다시 계산해야 하며, 어떤 일정을 미루고 어떤 일정을 유지해야 하는지 판단해야 한다.

우리 서비스는 이러한 과정을 AI가 대신 수행하여 사용자가 빠르게 새로운 하루 계획을 수립할 수 있도록 돕는다.

---

## 1.3 핵심 목표

MVP의 목표는 다음 세 가지이다.

- 사용자가 입력한 기존 일정을 기반으로 AI가 새로운 일정을 재설계한다.
- 재설계 결과를 기존 일정과 비교하여 제공한다.
- 사용자가 원하는 경우에만 새로운 일정으로 적용한다.

---

## 1.4 프로젝트 범위

본 프로젝트는 **MVP(Minimum Viable Product)** 개발을 목표로 한다.

MVP에서는 하루 단위 일정 재설계 기능을 우선 구현하며, 로그인 및 데이터베이스 기능은 포함하지 않는다.

---

# 2. MVP 범위

## 2.1 구현 기능

### 일정 관리

- 일정 추가
- 일정 조회

(후순위)

- 일정 수정
- 일정 삭제

---

### 사용자 설정

(이거는 구현보다는 값으로 정해두기, 시간 남으면 구현)

- 기상 시간
- 취침 시간
- 기본 휴식 시간
- 최소 식사 시간

---

### 돌발 상황 입력

사용자는 자연어 형태로 현재 상황을 입력할 수 있다.

예시

```
프로그래밍 과제가 두 시간 더 걸릴 것 같아.

오늘 회의가 한 시간 늦어졌어.

몸이 안 좋아서 한 시간 정도 쉬어야 할 것 같아.

새로운 과제가 생겼어.
```

---

### AI 일정 재설계

사용자의 일정과 현재 상황을 기반으로 OpenAI API를 호출하여 새로운 일정을 생성한다.

AI는 정해진 JSON 형식으로만 응답한다.

---

### 일정 비교

사용자는

- 기존 일정
- AI가 제안한 일정

을 동시에 확인할 수 있다.

변경된 일정은 이동, 연장, 단축 등의 형태로 표시한다.

---

### 일정 적용

사용자가 적용 버튼을 누르면 AI가 제안한 일정으로 교체된다.

---

## 2.2 MVP 제외 기능

다음 기능은 MVP에서 구현하지 않는다.

- 회원가입
- 로그인
- 데이터베이스
- 여러 기기 동기화
- 일정 공유
- Google Calendar 연동
- 푸시 알림
- 음성 입력
- 장기 일정 관리
- 사용자 행동 학습

---

## 2.3 데이터 저장 방식

MVP에서는 사용자 데이터를 브라우저의 **localStorage**에 저장한다.

저장 대상은 다음과 같다.

- 일정
- 사용자 설정
- 최근 AI 재설계 결과

OpenAI API Key는 브라우저에 저장하지 않으며, 서버 환경 변수에서 관리한다.

---

# 3. 시스템 아키텍처

## 3.1 전체 구조

```
사용자

↓

Frontend (Next.js)

↓

Backend API (Next.js Route Handler)

↓

OpenAI API

↓

응답 검증

↓

Frontend

↓

localStorage 저장
```

---

## 3.2 처리 흐름

### 1단계

사용자가 오늘의 일정을 입력한다.

↓

### 2단계

일정이 ScheduleEvent 형식으로 변환된다.

↓

### 3단계

일정이 localStorage에 저장된다.

↓

### 4단계

사용자가 돌발 상황을 입력한다.

↓

### 5단계

프론트엔드가 기존 일정과 사용자 입력을 서버 API로 전달한다.

↓

### 6단계

서버 API가 OpenAI API를 호출한다.

↓

### 7단계

OpenAI가 새로운 일정을 JSON 형태로 반환한다.

↓

### 8단계

서버가 응답을 검증한다.

↓

### 9단계

프론트엔드가 기존 일정과 새로운 일정을 비교하여 표시한다.

↓

### 10단계

사용자가 적용 버튼을 누르면 새로운 일정이 localStorage에 저장된다.

---

## 3.3 구성 요소

### Frontend

역할

- 일정 관리
- 사용자 입력
- 결과 화면 표시
- localStorage 관리
- API 호출

---

### Backend

역할

- API 요청 처리
- OpenAI API 호출
- 응답 검증
- 오류 처리

Backend는 사용자 데이터를 저장하지 않는다.

---

### OpenAI

역할

- 사용자 입력 해석
- 일정 우선순위 분석
- 일정 재배치
- 변경 이유 생성
- Structured Output(JSON) 생성

---

# 4. 기술 스택

| 분야 | 기술 |
| --- | --- |
| Framework | Next.js |
| Language | TypeScript |
| UI | React |
| Styling | Tailwind CSS |
| UI Component | shadcn/ui |
| Validation | Zod |
| Storage | localStorage |
| AI | OpenAI API |
| AI Output | Structured Outputs |
| Deployment | Vercel |
| Collaboration | GitHub |
| Documentation | Notion |
| Design | Figma |

---

# 5. 프로젝트 구조

```
src
│
├── app
│   ├── api
│   │   └── reschedule
│   │       └── route.ts
│   ├── layout.tsx
│   └── page.tsx
│
├── components
│   ├── schedule
│   ├── calendar
│   ├── reschedule
│   └── common
│
├── lib
│   ├── openai
│   ├── prompts
│   ├── storage
│   ├── validation
│   └── utils
│
├── schemas
│
├── types
│
└── data
```

---

### 폴더 역할

#### app

페이지 및 API 구성

---

#### components

재사용 가능한 UI 컴포넌트

---

#### lib

비즈니스 로직

- OpenAI 호출
- Prompt
- localStorage
- Validation

---

#### schemas

Zod Schema 관리

---

#### types

공통 TypeScript 타입

---

#### data

Mock 데이터 관리

---

# 6. 데이터 모델 (Schema)

본 프로젝트의 모든 데이터는 공통 Schema를 기준으로 주고받는다.

프론트엔드, 백엔드, LLM은 동일한 데이터 구조를 사용하며, API 요청 및 응답은 모두 이 Schema를 따른다.

---

# 6.1 ScheduleEvent

사용자가 등록한 하나의 일정을 표현하는 객체이다.

## TypeScript

```tsx
export type Priority = "low" | "medium" | "high" | "fixed";

export type ScheduleStatus =
  | "scheduled"
  | "completed"
  | "postponed"
  | "cancelled";

export interface ScheduleEvent {
  id: string;
  title: string;
  date: string;
  startTime: string;
  endTime: string;
  priority: Priority;
  deadline: string;
}
```

---

## JSON 예시

```json
{
  "id": "event-001",
  "title": "운영체제 과제",
  "date": "2026-07-27",
  "startTime": "14:00",
  "endTime": "16:00",
  "priority": "high",
  "deadline": "2026-08-01"
}
```

---

## 필드 설명

| 필드 | 설명 |
| --- | --- |
| id | 일정 고유 ID |
| title | 일정 이름 |
| date | 일정 날짜 |
| startTime | 시작 시간 |
| endTime | 종료 시간 |
| priority | 우선순위 |
| deadline | 마감 시간 |
---

# 6.2 UserPreferences

사용자의 기본 생활 패턴을 저장하는 객체이다.

AI는 일정을 재설계할 때 해당 정보를 참고한다.

## TypeScript

```tsx
export interface UserPreferences {
  wakeUpTime: string;
  sleepTime: string;
  minimumMealMinutes: number;
  timezone: "Asia/Seoul";
}
```

---

## JSON 예시

```json
{
  "wakeUpTime": "07:00",
  "sleepTime": "00:00",
  "minimumMealMinutes": 30,
  "timezone": "Asia/Seoul"
}
```

---

# 6.3 RescheduleRequest

프론트엔드가 백엔드 API로 전달하는 요청 형식이다.

프론트엔드는 localStorage에 저장된 일정과 사용자 입력을 함께 전달한다.

## TypeScript

```tsx
export interface RescheduleRequest {
  requestId: string;
  requestedAt: string;
  currentDate: string;
  currentTime: string;
  timezone: "Asia/Seoul";
  userInput: string;
  preferences: UserPreferences;
  schedules: ScheduleEvent[];
}
```

---

## JSON 예시

```json
{
  "requestId": "request-001",
  "requestedAt": "2026-07-27T15:20:00+09:00",
  "currentDate": "2026-07-27",
  "currentTime": "15:20",
  "timezone": "Asia/Seoul",
  "userInput": "과제가 두 시간 더 걸릴 것 같아.",
  "preferences": {},
  "schedules": []
}
```

---

# 6.4 Interpretation

LLM이 사용자 입력을 해석한 결과이다.

사용자의 자연어를 구조화된 데이터로 변환하여 이후 일정 재설계에 활용한다.

---

## DisruptionType

```tsx
export type DisruptionType =
  | "new_event"
  | "duration_increase"
  | "duration_decrease"
  | "time_change"
  | "event_cancelled"
  | "event_completed_early"
  | "unavailable_time"
  | "condition_change"
  | "other";
```

---

## TypeScript

```tsx
export interface Interpretation {
  type: DisruptionType;

  targetEventId: string | null;

  title: string | null;

  additionalDurationMinutes: number | null;

  newDurationMinutes: number | null;

  newStartTime: string | null;

  newEndTime: string | null;

  description: string;

  confidence: number;
}
```

---

## JSON 예시

```json
{
  "type": "duration_increase",
  "targetEventId": "event-001",
  "title": null,
  "additionalDurationMinutes": 120,
  "newDurationMinutes": null,
  "newStartTime": null,
  "newEndTime": null,
  "description": "과제 수행 시간이 예상보다 2시간 증가",
  "confidence": 0.98
}
```

---

# 6.5 ScheduleChange

AI가 기존 일정을 어떻게 변경했는지를 기록하는 객체이다.

프론트엔드에서는 변경 내역을 사용자에게 보여줄 때 사용한다.

---

## ChangeAction

```tsx
export type ScheduleChangeAction =
  | "kept"
  | "moved"
  | "extended"
  | "shortened"
  | "split"
  | "postponed"
  | "cancelled"
  | "created";
```

---

## TypeScript

```tsx
export interface ScheduleChange {
  eventId: string;

  action: ScheduleChangeAction;

  previousStartTime: string | null;

  previousEndTime: string | null;

  newStartTime: string | null;

  newEndTime: string | null;

  reason: string;
}
```

---

## JSON 예시

```json
{
  "eventId": "event-003",
  "action": "moved",
  "previousStartTime": "18:00",
  "previousEndTime": "19:00",
  "newStartTime": "20:00",
  "newEndTime": "21:00",
  "reason": "우선순위가 높은 일정을 먼저 배치"
}
```

---

# 6.6 RescheduleResponse

OpenAI가 생성하고 백엔드가 검증한 최종 응답 형식이다.

프론트엔드는 해당 객체를 이용하여 비교 화면을 구성한다.

---

## TypeScript

```tsx
export interface RescheduleResponse {
  requestId: string;
	responseId: string;
  success: boolean;
  interpretation: Interpretation;
  summary: string;
  rescheduledEvents: ScheduleEvent[];
  changes: ScheduleChange[];
  warnings: string[];
  requiresUserConfirmation: boolean;
}
```

---

## JSON 예시

```json
{
  "requestId": "request-001",
  "responseId": "response-001"
  "success": true,
  "interpretation": {},
  "summary": "과제 시간이 증가하여 저녁 운동을 뒤로 이동했습니다.",
  "rescheduledEvents": [],
  "changes": [],
  "warnings": [],
  "requiresUserConfirmation": true
}
```

---

# 6.7 데이터 흐름

프로젝트에서 데이터는 다음 순서로 전달된다.

```
사용자 일정 입력

↓

ScheduleEvent 생성

↓

localStorage 저장

↓

RescheduleRequest 생성

↓

Backend API

↓

OpenAI API

↓

RescheduleResponse 반환

↓

Frontend 비교 화면

↓

사용자 승인

↓

localStorage 업데이트
```

---

# 6.8 데이터 검증

모든 요청 및 응답 데이터는 **Zod Schema**를 사용하여 검증한다.

검증 대상은 다음과 같다.

- ScheduleEvent
- UserPreferences
- RescheduleRequest
- Interpretation
- ScheduleChange
- RescheduleResponse

검증에 실패한 데이터는 사용하지 않으며, 백엔드에서 오류를 반환한다.

---

# 7. OpenAI 일정 재설계

본 프로젝트에서 OpenAI는 사용자의 자연어 입력을 분석하고, 기존 일정을 기반으로 새로운 일정을 생성하는 역할을 수행한다.

모든 응답은 **Structured Outputs**를 사용하여 사전에 정의된 JSON Schema 형식으로 반환한다.

---

# 7.1 처리 과정

일정 재설계는 다음 순서로 진행된다.

```
사용자 입력

↓

현재 일정 조회

↓

RescheduleRequest 생성

↓

OpenAI API 호출

↓

Structured Output 생성

↓

응답 검증

↓

프론트엔드 전달
```

---

# 7.2 OpenAI 입력

OpenAI에는 다음 정보를 함께 전달한다.

| 입력 데이터 | 설명 |
| --- | --- |
| 현재 시각 | 이미 지난 일정 변경 방지 |
| 사용자 입력 | 돌발 상황 자연어 |
| 기존 일정 | 오늘 일정 전체 |
| 사용자 설정 | 기상, 취침, 휴식 시간 등 |

---

## 입력 예시

```json
{
  "currentTime": "15:20",
  "userInput": "운영체제 과제가 두 시간 더 걸릴 것 같아.",
  "preferences": {},
  "schedules": []
}
```

---

# 7.3 OpenAI 역할

OpenAI는 다음 작업을 수행한다.

### 1. 사용자 입력 해석

자연어를 구조화된 정보로 변환한다.

예시

```
"과제가 두 시간 더 걸릴 것 같아."

↓

duration_increase
```

---

### 2. 영향 받는 일정 분석

어떤 일정이 영향을 받는지 판단한다.

예시

- 이후 일정 이동
- 일정 단축
- 일정 연장
- 일정 취소
- 일정 분할

---

### 3. 일정 재배치

다음 조건을 고려하여 새로운 일정을 생성한다.

- 우선순위
- 고정 일정
- 마감 시간
- 최소 수행 시간
- 휴식 시간
- 사용자 설정

---

### 4. 변경 이유 생성

모든 일정 변경에는 이유를 포함한다.

예시

```
과제 수행 시간이 증가하여 운동 시간을 뒤로 이동했습니다.
```

---

### 5. Structured Output 생성

최종 결과를 JSON 형태로 반환한다.

---

# 7.4 Prompt 설계 원칙

Prompt는 일정을 생성하는 것이 아니라 **재설계**하는 것을 목표로 한다.

OpenAI는 다음 규칙을 반드시 따라야 한다.

1. 기존 일정을 최대한 유지한다.
2. 현재 시각 이전 일정은 변경하지 않는다.
3. 완료된 일정은 수정하지 않는다.
4. 고정 일정은 이동하지 않는다.
5. 일정 시간이 겹치지 않아야 한다.
6. 시작 시간은 종료 시간보다 빨라야 한다.
7. 일정 길이는 시작·종료 시간과 일치해야 한다.
8. 마감 시간이 있는 일정은 마감 이전에 배치한다.
9. 높은 우선순위 일정을 우선 유지한다.
10. 분할 가능한 일정만 분할한다.
11. 최소 수행 시간보다 짧게 줄이지 않는다.
12. 사용자의 취침 시간 이후에는 일정을 생성하지 않는다.
13. 기존 일정의 ID는 유지한다.
14. 새 일정만 새로운 ID를 생성한다.
15. 모든 변경에는 이유를 작성한다.
16. 재설계가 불가능한 경우 `success: false`를 반환한다.
17. JSON 이외의 텍스트는 출력하지 않는다.

---

# 7.5 Structured Outputs

본 프로젝트는 OpenAI Structured Outputs를 사용하여 응답 형식을 강제한다.

이를 통해 프론트엔드가 별도의 후처리 없이 바로 데이터를 사용할 수 있도록 한다.

```
LLM

↓

JSON Schema

↓

RescheduleResponse

↓

Frontend
```

---

## 응답 예시

```json
{
  "requestId": "request-001",
  "success": true,
  "interpretation": {},
  "summary": "과제 시간이 증가하여 운동 시간을 뒤로 이동했습니다.",
  "rescheduledEvents": [],
  "changes": [],
  "warnings": [],
  "requiresUserConfirmation": true
}
```

---

# 7.6 응답 검증

OpenAI 응답은 그대로 사용하지 않는다.

백엔드에서 다음 항목을 검증한 후 프론트엔드로 전달한다.

- JSON Schema
- 일정 시간 중복 여부
- 일정 길이 일치 여부
- 고정 일정 변경 여부
- 완료 일정 변경 여부
- 마감 시간 초과 여부

검증을 통과하지 못한 응답은 사용자에게 전달하지 않는다.

---

# 8. API 명세

프론트엔드와 백엔드는 REST API를 통해 통신한다.

MVP에서는 일정 재설계 API 하나만 사용한다.

---

# 8.1 일정 재설계 API

### Endpoint

```
POST /api/reschedule
```

---

### Request

```tsx
RescheduleRequest
```

---

### Response

```tsx
RescheduleResponse
```

---

## 처리 흐름

```
Frontend

↓

POST /api/reschedule

↓

Backend

↓

OpenAI API

↓

Response Validation

↓

Frontend
```

---

# 8.2 성공 응답

```
200 OK
```

```json
{
  "success": true,
  "summary": "...",
  "rescheduledEvents": [],
  "changes": []
}
```

---

# 8.3 요청 오류

```
400 Bad Request
```

```json
{
  "success": false,
  "error": {
    "code": "INVALID_REQUEST",
    "message": "잘못된 요청입니다."
  }
}
```

---

# 8.4 AI 응답 오류

```
422 Unprocessable Entity
```

```json
{
  "success": false,
  "error": {
    "code": "INVALID_AI_RESPONSE",
    "message": "AI 응답 검증에 실패했습니다."
  }
}
```

---

# 8.5 서버 오류

```
500 Internal Server Error
```

```json
{
  "success": false,
  "error": {
    "code": "SERVER_ERROR",
    "message": "일시적인 서버 오류가 발생했습니다."
  }
}
```

---

# 8.6 Backend 역할

백엔드는 다음 역할만 수행한다.

- API 요청 처리
- OpenAI API 호출
- Structured Output 검증
- 일정 유효성 검사
- 오류 처리

MVP에서는 사용자 데이터를 저장하지 않으며, 모든 일정 데이터는 프론트엔드에서 전달받아 처리한다.

---

---

# 9. localStorage 저장 규칙

MVP에서는 데이터베이스를 사용하지 않는다.

모든 사용자 데이터는 브라우저의 **localStorage**에 저장하며, 새로고침 이후에도 데이터를 유지한다.

---

## 9.1 저장 대상

localStorage에는 다음 데이터를 저장한다.

- 오늘의 일정
- 사용자 기본 설정
- 최근 AI 재설계 결과

---

## 9.2 저장 Key

| Key | 설명 |
| --- | --- |
| `ai-day-rescheduler:schedules` | 사용자의 일정 목록 |
| `ai-day-rescheduler:preferences` | 사용자 설정 |
| `ai-day-rescheduler:last-result` | 가장 최근 AI 재설계 결과 |

---

## 9.3 일정 저장 예시

```json
[
  {
    "id": "event-001",
    "title": "운영체제 과제",
    "date": "2026-07-27",
    "startTime": "14:00",
    "endTime": "16:00",
    "durationMinutes": 120,
    "priority": "high",
    "isFixed": false,
    "canSplit": true,
    "deadline": "2026-07-27T23:59:00+09:00",
    "minimumDurationMinutes": 60,
    "status": "scheduled",
    "memo": "오늘 제출"
  }
]
```

---

## 9.4 사용자 설정 저장 예시

```json
{
  "wakeUpTime": "07:00",
  "sleepTime": "00:00",
  "minimumMealMinutes": 30,
  "defaultBreakMinutes": 10,
  "preserveCompletedEvents": true,
  "allowPostponeLowPriority": true,
  "allowShortenFlexibleEvents": true,
  "timezone": "Asia/Seoul"
}
```

---

## 9.5 AI 응답 저장 예시

사용자가 마지막으로 확인한 AI 재설계 결과를 저장한다.

이를 통해 새로고침 이후에도 비교 화면을 유지할 수 있다.

```json
{
  "requestId": "request-001",
  "success": true,
  "summary": "과제 시간이 증가하여 운동 시간을 뒤로 이동했습니다.",
  "rescheduledEvents": [],
  "changes": [],
  "warnings": [],
  "requiresUserConfirmation": true
}
```

---

## 9.6 데이터 처리 흐름

```
사용자 입력

↓

localStorage 저장

↓

AI 재설계 요청

↓

AI 응답 저장

↓

사용자 승인

↓

일정 업데이트
```

---

# 10. 역할 분담

프로젝트는 프론트엔드, 백엔드, LLM 개발을 중심으로 역할을 분담하여 진행한다.

---

## 도영 (Backend)

### 담당 업무

- Backend 서버 개발
- API 설계 및 구현
- OpenAI API 연동
- 요청 및 응답 처리
- Structured Output 검증
- Zod Validation
- API 오류 처리

### 주요 산출물

- `/api/reschedule`
- OpenAI 호출 모듈
- Validation 모듈
- API 명세 구현

---

## 승민 · 서준(LLM / AI)

### 담당 업무

- Structured Outputs 설계
- 일정 재설계 로직 설계
- 사용자 입력 해석
- 일정 우선순위 판단
- 일정 변경 규칙 설계

- Prompt Engineering
- AI 응답 품질 개선

### 주요 산출물

- System Prompt
- User Prompt
- JSON Schema
- 일정 재설계 규칙
- OpenAI 응답 설계

---

## 민찬 · 서준(Frontend)

### 담당 업무

[메인] 민찬

- 캘린더 UI
    - 1안 → 오늘 걸 내일로 미뤄, 일주일 띄우기
    - 2안 → 그게 잘 안되면 하루만 띄우기
    - 일정 추가
        - JSON 데이터 별로 정렬해서 띄우기
    - 일정 삭제
- 대화창
    - 사용자 입력 화면
    - AI 응답을 띄우기
    - 결과 확인 버튼 만들기
- 일정 추가, 일정 삭제 버튼

[팝업창] 서준

- 일정 비교 화면 (기존, 1안, 2안 + @)
    - 대화창의 결과 확인 버튼(결과 확인) → 팝업창으로 띄우기
        - (버튼) 선택하면 캘린더에 반영
- 일정 추가, 일정 삭제
    - 받아야 할 데이터는 JSON 예시 참고

- localStorage 관리
- UI/UX 개선

### 주요 산출물

- 일정 CRUD 화면
- 비교 화면
- 설정 화면
- AI 결과 화면

---

## 공동 작업

모든 팀원이 함께 수행하는 작업은 다음과 같다.

- GitHub 협업
- 코드 리뷰
- 기능 테스트
- 버그 수정
- UI 피드백
- 최종 발표 준비

---

# 11. 개발 일정

프로젝트는 다음 순서로 개발을 진행한다.

| 단계 | 주요 내용 |
| --- | --- |
| 1 | 개발 환경 구축 |
| 2 | 공통 Schema 설계 |
| 3 | 프론트엔드 UI 구현 |
| 4 | 백엔드 API 구현 |
| 5 | OpenAI 연동 |
| 6 | Frontend-Backend 통합 |
| 7 | 기능 테스트 및 오류 수정 |
| 8 | 배포 및 발표 준비 |

---

## 개발 흐름

```
환경 구축

↓

Schema 설계

↓

Frontend 개발

↓

Backend 개발

↓

OpenAI 연동

↓

통합 테스트

↓

배포
```

---

# 12. MVP 완료 기준

다음 기능이 모두 정상적으로 동작하면 MVP를 완료한 것으로 판단한다.

## 필수 기능

- 일정 생성
- 일정 조회
- 일정 수정
- 일정 삭제
- 사용자 설정 저장
- localStorage 저장
- OpenAI API 호출
- AI 일정 재설계
- 기존 일정과 AI 일정 비교
- AI 일정 적용

---

## 품질 기준

- 일정이 시간상 겹치지 않는다.
- JSON 응답이 Schema를 만족한다.
- 오류 발생 시 적절한 메시지를 반환한다.
- 새로고침 후에도 일정이 유지된다.
- 사용자 승인 없이 일정이 변경되지 않는다.

---

# 13. 추후 확장 계획

MVP 이후에는 다음 기능을 추가하여 서비스를 확장할 계획이다.

## 사용자 기능

- 회원가입 및 로그인
- 사용자별 데이터 관리
- 여러 기기 간 동기화
- 장기 일정 관리
- 반복 일정 지원
- 일정 공유 기능

---

## AI 기능

- 사용자 일정 패턴 학습
- 개인화된 일정 재설계
- 일정 추천 기능
- 자연어 일정 생성
- 음성 입력 지원
- 멀티모달 입력 지원

---

## 외부 서비스 연동

- Google Calendar 연동
- Apple Calendar 연동
- Outlook Calendar 연동
- Slack 알림
- Discord 알림

---

## 기술 확장

- PostgreSQL 기반 데이터 관리
- Supabase 또는 Firebase 연동
- 사용자 인증 시스템
- 서버 캐싱
- AI 응답 캐싱
- 실시간 동기화(WebSocket)

---

# 부록. 프로젝트 전체 흐름

```
사용자

↓

오늘 일정 입력

↓

localStorage 저장

↓

돌발 상황 입력

↓

RescheduleRequest 생성

↓

POST /api/reschedule

↓

Backend

↓

OpenAI API

↓

Structured Outputs

↓

Response Validation

↓

RescheduleResponse

↓

Frontend 비교 화면

↓

사용자 승인

↓

localStorage 업데이트
```

---

전체 파일 구조

```
src
│
├── app
│   ├── api
│   │   └── reschedule
│   │       └── route.ts
│   │
│   ├── page.tsx
│   ├── layout.tsx
│   └── globals.css
│
├── components
│   ├── calendar
│   ├── schedule
│   ├── compare
│   ├── settings
│   └── common
│
├── lib
│   ├── openai
│   ├── storage
│   ├── validation
│   └── utils
│
├── schemas
│
├── types
│
├── hooks
│
├── constants
│
└── data
```

---

# 폴더별 역할

## app

Next.js의 App Router 구조를 담당하는 폴더이다.

페이지 렌더링과 API 엔드포인트를 정의하며, 프로젝트의 진입점 역할을 수행한다.

### 주요 파일

- **page.tsx** : 메인 일정 관리 화면
- **layout.tsx** : 공통 레이아웃
- **globals.css** : 전역 스타일
- **api/reschedule/route.ts** : AI 일정 재설계 API

---

## components

사용자에게 보여지는 모든 UI 컴포넌트를 관리하는 폴더이다.

기능별로 컴포넌트를 분리하여 재사용성과 유지보수성을 높인다.

### calendar

캘린더 화면을 구성하는 컴포넌트

예시

- Calendar
- TimeGrid
- EventCard

---

### schedule

일정 CRUD와 관련된 컴포넌트

예시

- ScheduleForm
- ScheduleList
- ScheduleItem

---

### compare

기존 일정과 AI가 재설계한 일정을 비교하는 화면

예시

- CompareView
- BeforeSchedule
- AfterSchedule
- ChangeList

---

### settings

사용자 설정 화면

예시

- PreferenceForm

---

### common

여러 화면에서 공통으로 사용하는 컴포넌트

예시

- Button
- Dialog
- Input
- Loading

---

## lib

프로젝트의 핵심 비즈니스 로직을 관리하는 폴더이다.

UI와 독립적인 기능을 구현하며, 여러 컴포넌트에서 공통으로 사용된다.

---

### openai

OpenAI API와 관련된 기능을 관리한다.

주요 역할

- OpenAI Client 생성
- Prompt 관리
- 일정 재설계 요청
- Structured Outputs 처리

---

### storage

브라우저 localStorage를 관리한다.

주요 역할

- 일정 저장
- 일정 조회
- 사용자 설정 저장
- AI 응답 저장

---

### validation

API 요청 및 응답 데이터를 검증한다.

주요 역할

- Zod Schema 검증
- OpenAI 응답 검증
- 일정 유효성 검사

---

### utils

공통 유틸리티 함수를 관리한다.

예시

- 날짜 계산
- 시간 계산
- 일정 정렬
- 문자열 처리

---

## schemas

Zod Schema를 정의하는 폴더이다.

프론트엔드와 백엔드에서 동일한 데이터 구조를 사용하기 위해 모든 데이터 검증 규칙을 이곳에서 관리한다.

예시

- Schedule Schema
- Request Schema
- Response Schema

---

## types

프로젝트에서 사용하는 TypeScript 타입을 정의한다.

주요 타입

- ScheduleEvent
- UserPreferences
- RescheduleRequest
- Interpretation
- ScheduleChange
- RescheduleResponse

---

## hooks

React Custom Hook을 관리한다.

상태 관리와 API 호출 로직을 재사용하기 위해 사용한다.

예시

- useSchedule
- usePreferences
- useReschedule

---

## constants

프로젝트에서 공통으로 사용하는 상수 값을 관리한다.

예시

- localStorage Key
- 우선순위 값
- 상태(Enum)
- 기본 설정 값

---

## data

개발 및 테스트에 사용하는 Mock 데이터를 관리한다.

실제 서비스에서는 테스트 데이터 또는 초기 데이터를 저장하는 용도로 사용한다.

예시

- Mock 일정 데이터
- 테스트용 사용자 설정

---

# 폴더 간 관계

```
app
 │
 ├── components (UI)
 │
 ├── hooks (상태 관리)
 │
 ├── lib (비즈니스 로직)
 │      │
 │      ├── storage (localStorage)
 │      ├── openai (AI 호출)
 │      └── validation (데이터 검증)
 │
 ├── schemas (Zod)
 │
 ├── types (TypeScript 타입)
 │
 ├── constants (공통 상수)
 │
 └── data (Mock 데이터)
```

# 담당 폴더 요약

| 담당 | 주요 폴더 |
| --- | --- |
| **Frontend (민찬 · 서준)** | `components`, `hooks`, `storage`, `constants`, `app/page.tsx` |
| **Backend (도영)** | `app/api`, `validation`, `types`, `route.ts` |
| **LLM (승민)** | `openai`, `schemas`, Prompt, AI 재설계 로직 |
| **공동** | `utils`, `data`, GitHub 통합 및 테스트 |

---

# GitHub 협업 규칙

프로젝트는 GitHub를 이용하여 협업하며, **Git Flow를 단순화한 브랜치 전략**을 사용한다.

## 브랜치 전략

기본 브랜치는 `main`으로 유지하며, 모든 팀원은 자신의 작업을 별도의 브랜치에서 진행한다.

브랜치 이름은 담당 역할 또는 기능을 기준으로 작성한다.

### 브랜치 예시

```
main

frontend/minchan
frontend/seojun

backend/doyoung

llm/seungmin
```

또는 기능 단위로 작업할 경우

```
feature/calendar-ui
feature/reschedule-api
feature/prompt-engineering
feature/settings
```

---

## 개발 절차

모든 기능 개발은 다음 절차를 따른다.

```
main 브랜치 최신 내용 Pull

↓

개인 브랜치에서 개발

↓

기능 완료

↓

Commit 및 Push

↓

Pull Request(PR) 생성

↓

팀원 코드 리뷰

↓

Merge

↓

main 업데이트
```

---

## Pull Request 규칙

모든 기능은 **직접 `main` 브랜치에 Push하지 않는다.**

기능 개발이 완료되면 GitHub에서 Pull Request(PR)를 생성한다.

PR에는 다음 내용을 포함한다.

- 구현한 기능
- 변경된 파일
- 테스트 결과
- 추가 확인 사항(있는 경우)

팀원들의 확인 후 `main` 브랜치로 Merge한다.

---

## Commit Message 규칙

Commit Message는 변경 내용을 쉽게 확인할 수 있도록 작성한다.

예시

```
feat: 일정 CRUD 구현

feat: OpenAI 일정 재설계 API 추가

feat: Prompt Engineering 개선

fix: 일정 겹침 오류 수정

refactor: Schedule 컴포넌트 구조 개선

docs: 개발 명세서 수정
```

---

## 코드 리뷰 규칙

Pull Request가 생성되면 팀원들은 다음 사항을 확인한다.

- 기능이 정상적으로 동작하는가
- 코드 스타일이 일관적인가
- 불필요한 코드가 없는가
- 기존 기능에 영향을 주지 않는가

리뷰가 완료되면 `main` 브랜치로 Merge한다.

---

## 담당 영역

| 담당 | 주 작업 브랜치 | 주요 작업 |
| --- | --- | --- |
| Frontend (민찬 · 서준) | `frontend/*` | UI 구현, API 연동, localStorage |
| Backend (도영) | `backend/*` | API 개발, Validation, OpenAI 연동 |
| LLM (승민) | `llm/*` | Prompt Engineering, Schema, AI 로직 |

---